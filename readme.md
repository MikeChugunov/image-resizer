# Batch image resizer на gulp
Использование:
После клонирования выполнить `npm install`, настроить необходимые действия,
суффиксы и разрешения в `gulpfile.js`, выполнить `gulp` в консоли.

По умолчанию берет изображения из `./src`
